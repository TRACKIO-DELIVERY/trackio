name: Deploy to Production

on:
    repository_dispatch:
        types: [order-service-updated]

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name : Checkout repository
              uses: actions/checkout@v2